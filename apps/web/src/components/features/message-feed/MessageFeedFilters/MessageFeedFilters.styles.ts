import type { SxProps, Theme } from '@mui/material/styles';

export const messageFeedFiltersStyles = {
  badge: {
    alignSelf: 'flex-start',
    width: 'fit-content',
  } satisfies SxProps<Theme>,
  trigger: {
    alignSelf: 'flex-start',
  } satisfies SxProps<Theme>,
};
