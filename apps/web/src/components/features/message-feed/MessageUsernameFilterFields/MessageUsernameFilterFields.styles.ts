import type { SxProps, Theme } from '@mui/material/styles';

export const messageUsernameFilterFieldsStyles = {
  autocomplete: {
    width: '100%',
  } satisfies SxProps<Theme>,
  dismissButton: {
    alignSelf: 'flex-start',
  } satisfies SxProps<Theme>,
  root: {
    width: '100%',
  } satisfies SxProps<Theme>,
};
