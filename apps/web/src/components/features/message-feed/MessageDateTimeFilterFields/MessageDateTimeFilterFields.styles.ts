import type { SxProps, Theme } from '@mui/material/styles';

export const messageDateTimeFilterFieldsStyles = {
  dismiss: {
    alignSelf: 'flex-start',
  } satisfies SxProps<Theme>,
  input: {
    width: '100%',
  } satisfies SxProps<Theme>,
  root: {
    width: '100%',
  } satisfies SxProps<Theme>,
};
